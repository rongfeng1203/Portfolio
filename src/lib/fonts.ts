/* ============================================================
   src/lib/fonts.ts
   Font variable names only. The actual font stacks live in
   src/app/globals.css so the site can build without fetching
   remote font files.
   ============================================================ */

export const fontVariables = "";
